v0.98.6 - Build 103 (5/4/2026 11:10 PM EST)
-----------------------------------------
  • [LINUX] Window centering fixed — main window and secondary panels now appear centered on first launch on GNOME/Mutter compositors (forced X11 backend restores reliable window positioning lost in Electron 41's Wayland auto-detection change)
  • [ALL] Open folder from expanded download history panel now works correctly (history window was missing API bridge that prevented folder actions)
  • [ALL] Video thumbnail aspect ratio corrected — widescreen (16:9) videos now display at proper proportions instead of appearing stretched vertically
  • [ALL] Version references corrected — footer version pill and What's New header now update correctly during version bumps (scripts coverage gap fixed)
  • [WINDOWS] Portable: Update Plugins pane now accessible in portable mode with safety disclaimer for plugin recovery

  Known issues:
  • [LINUX] Cookies browse dialog does not open on Linux. Linux-specific fix planned for upcoming release.
  • [LINUX] Virtual machine environments (VMware, VirtualBox, etc.) may experience display rendering issues. The app is primarily designed and tested for bare-metal Linux installations.

v0.98.5 - Build 102 (5/2/2026 2:25 AM EST)
-----------------------------------------
  • [ALL] Electron runtime upgraded to v41.5.0 (security and compatibility improvements; v29 line is end-of-life with no security backports — this upgrade incorporates accumulated Chromium security fixes and backported CVEs from the Electron security advisory line)
  • [ALL] electron-builder updated to v26.8.1 (build toolchain compatibility with Electron 41)
  • [LINUX] Improved window initialization reliability — main window now appears consistently when launched from file manager or application launcher on GNOME/Mutter compositors (Ubuntu 24 and similar distros)

  Known issues:
  • [ALL] Open folder from the expanded download history panel does not work — use the hamburger menu's open folder action instead. Fix planned for next release.
  • [LINUX] Splash window and certain secondary panels (themes, expanded history, footer link popups) may appear off-center (typically top-left) on first display. Functional behavior unaffected. Targeted fix planned for next release.
  • [LINUX] Cookies browse dialog does not open on Linux. Linux-specific issue, fix planned for upcoming release.


v0.98 - Build 101 (4/28/2026 1:47 AM EST)
-----------------------------------------
  • [ALL] URL input pane redesigned — compact unified buttons (28px), textarea shrinks to 3 lines by default and auto-grows as URLs are added, "Max videos" caption replaces "Videos"
  • [ALL] Drag and drop URLs directly into the URL textarea — visual drag-over feedback, filters comment lines, appends cleanly
  • [ALL] Keyboard shortcuts — paste-and-fetch (Ctrl+V / ⌘V), clear textarea (Esc), trigger Fetch Info (Ctrl+Enter / ⌘Return)
  • [ALL] All Themes window redesigned — categories moved to left sidebar with collapsible sections (Style, Vibe, Culture, Other); search bar compact (expands on focus); category counts shown
  • [ALL] Download subtitles toggle in Advanced panel — embeds English subtitles (manual preferred, auto-generated fallback) directly into video file; no separate .srt files
  • [ALL] Embed metadata toggle in Advanced panel (ON by default) — embeds thumbnail, chapter markers, and title/artist/date metadata into downloaded videos
  • [ALL] Output format selector in Advanced panel — switch between MP4 (default) and MKV container for video downloads
  • [ALL] Video quality picker expanded — 8K (4320p), 4K (2160p), and 2K (1440p) added with High Resolution / Standard tier separators
  • [ALL] Audio quality picker expanded — M4A (256 kbps) and OPUS (128 kbps, 192 kbps) formats added alongside MP3 and FLAC with format section dividers
  • [ALL] Checksum verification for auto-updates — SHA256 computed during build, embedded in JSON feed, verified before extracting installer; tampering detected and blocked
  • [ALL] Distribution zips are now unencrypted — EGMd.zip and EGMdM.zip are standard zips; pyzipper dependency removed; SHA256 replaces password as integrity mechanism
  • [ALL] What's New modal now reads release notes live from the JSON feed — no more hardcoded bullets; shows "Loading…" while fetching, graceful fallback if offline
  • [ALL] 14 new themes (120 total: 110 permanent + 10 seasonal) — Destiny characters (Cayde-6, Zavala, Savathûn, The Witness), Hyrule, Velvet Rose, Nocturnix, Hanami, Sugar Riot, Sundrop, Dream Dust, First Light, Neon Kiss, Prism
  • [ALL] 6 Mood themes added — Melancholy, Euphoria, Tension, Serenity, Nostalgia, Focus
  • [ALL] Theme CSS is now a single shared source — main app and themes window both include theme_styles.html; new themes apply everywhere automatically
  • [ALL] Theme validation switched from static allowlist to regex — future themes work without code changes
  • [ALL] Mac auto-update now opens the download page in the browser — no more confusing error on non-installer builds
  • [ALL] Double update toast guard added — auto-check on launch no longer fires twice
  • [ALL] Security hardening — URL scheme validation on all API endpoints, IPC input allowlists, concurrent_fragments clamped 1–16, video_height allowlisted, open-folder path safety
  • [ALL] App size reduced ~40MB — Python bundle stripped of pip, setuptools, idlelib, tkinter, pycache on Mac and Linux builds
  • [WINDOWS] Desktop shortcut button shows confirmation dialog; detects if shortcut already exists and shows "already exists" toast instead of overwriting
  • [ALL] Reinstall ffmpeg and Reinstall Deno now have distinct icons (▶ and ⬡) for quick visual identification
  • [ALL] Advanced panel section labels removed — cleaner layout for power users; buttons are self-explanatory
  • [ALL] Footer spacing tightened between Advanced panel and footer
  • [WINDOWS] Windows portable edition — EGMd-portable.zip runs from any folder or USB stick; no installer, no registry, no system changes; settings and history stay in data/ inside the portable folder; auto-update disabled
  • [WINDOWS] Portable variant has its own update feed (egm-portable-version.json) and update UI is hidden automatically when running portable
  • [WINDOWS] Build pipeline now produces both installer and portable zips in one pass

v0.97.5 - Build 100 (4/26/2026 10:25 PM EST)
-----------------------------------------
  • [ALL] Mac auto-update — now opens download page in browser instead of attempting installer launch; no false error
  • [ALL] Footer spacing — gap between Advanced panel and footer reduced to match top spacing
  • [ALL] Advanced panel redesigned — sectioned layout with General, Settings, Plugins, System labels and uniform button rows
  • [ALL] Security hardening — URL scheme validation on all API endpoints, IPC input allowlists, concurrent_fragments clamped, video_height allowlisted, open-folder path safety
  • [ALL] App size reduction — Python bundle stripped of pip, setuptools, idlelib, tkinter, pycache (~40MB saved on Linux/Mac)
  • [ALL] 14 new themes: Music (Vinyl, Lo-Fi, Jazz Club, Studio Midnight, Rave, Acoustic), Nature (Bioluminescent, Aurora, Desert Dusk, Volcanic), Cities (Tokyo Neon, Havana, Dubai Midnight, Favela Sunset)
  • [ALL] Music, Cities, Moods category tabs added to themes window; total theme count reaches 100

v0.97 - Build 99 (4/26/2026 12:00 PM EST)
-----------------------------------------
  • [ALL] 76 themes total — 66 permanent across 8 categories plus 10 seasonal easter eggs that automatically appear near their holiday and hide after
  • [ALL] 46 new permanent themes across 6 categories: Nature, Consoles, Characters, Movies, Retro, Experimental — each with a unique static background texture
  • [ALL] 10 seasonal easter egg themes (Valentine's, St. Patrick's, Easter, 4th of July, Halloween, Thanksgiving, Christmas, New Year, Diwali, Hanukkah) — auto-show 30 days before, auto-hide 30 days after
  • [ALL] Theme picker in hamburger panel now shows 10 themes only (current pinned first, 9 most recently used)
  • [ALL] View All Themes window — separate window with category tabs (Dark, Light, Nature, Consoles, Characters, Movies, Retro, Experimental, Seasonal, Imported), search, and grid layout; remembers size and position
  • [ALL] Themes window and Download History window now fully themed — inherit the active theme with live update on change
  • [ALL] Vertical video thumbnails (TikTok, Shorts, Reels) display with the correct portrait aspect ratio
  • [ALL] Select video quality before bulk downloads — choose Best, 1080p, 720p, 480p, or 360p
  • [ALL] Download history updates instantly when a download completes — no need to reopen the panel
  • [ALL] Install Updates button now only appears when at least one plugin update toggle is ON; resets after install completes
  • [ALL] Cookies section — helper text "For age-restricted or login-required content" added before browser extension pills
  • [ALL] Settings panel — download folder only; compacted
  • [ALL] Advanced panel — Check updates on launch toggle (left) and Quit when done toggle (right) on same top row
  • [ALL] 18 additional themes: 6 Consoles (SNES, N64, GameCube, Virtual Boy, Saturn, Neo Geo), 6 Characters (Chosen Undead, Vault Dweller, N7, Dragonborn, Big Boss, Lara Croft), 6 Experimental (Holographic, Y2K, Liminal, Signal, Corrupted, Neon Noir)
  • [ALL] DMG and Cobalt Rush themes reworked for proper readability
  • [WINDOWS] App icon fixed — now uses icon.ico for crisp title bar rendering


v0.96 - Build 98 (4/25/2026 10:00 PM EST)
-----------------------------------------
  • [ALL] Download History — hamburger (B4 grid) button in header replaces theme picker; History + Themes tabs; sliding window 10×10 with collapsed pills; ⤢ Open full history window; per-entry re-add, open folder, remove; count badge
  • [ALL] Full history window — search, filter (All/Video/Audio/FLAC), sort, paginated table with format badges and file sizes; Clear All; persists across launches in egm_history.json
  • [ALL] Theme import/export — export active theme as .json file via native Save As dialog; import with validation of 12 required CSS vars; preview modal before applying; custom theme slot in picker
  • [ALL] 10 themes total (4 new themes added)
  • [ALL] Settings export/import via native Save As/Open File dialog (replaces clipboard)
  • [ALL] Auto-update check on launch (silent — only acts if update found)
  • [ALL] Footer restructured — version pill + Check for Updates on one row; ♥ Support EGM donate pill below
  • [ALL] Check for Updates moved to footer only; removed from header
  • [ALL] Up to date and error update states now show as toasts instead of modals
  • [ALL] Cookies section — single line with Add-on: Chrome/Edge/Brave and Firefox browser pills
  • [ALL] Desktop shortcut moved to Advanced settings panel
  • [ALL] Unified settings panel with 4 accordion sections
  • [ALL] Compact plugin cards/rows
  • [ALL] Cancel button fix
  • [ALL] Advanced panel toggle fix
  • [WINDOWS] Auto-update: silent background download → toast with Restart Now button (8s)
  • [WINDOWS] NSIS installer finish page — Add shortcut to desktop checkbox (unchecked by default)
  • [MAC] Auto-update: launch check shows update available modal
  • [LINUX] Auto-update: launch check shows toast with Download link (no auto-install)


v0.95 - Build 97 (4/25/2026 3:32 PM EST)
-----------------------------------------
  • [ALL] New logo — Transmission Halo design, inline SVG using CSS variables; adapts automatically to every theme without any image files
  • [ALL] 6 themes — Void (default), Eclipse (deep purple), Ghost (GitHub green), Frost (cool blue-gray light), Bone (warm cream light), Haze (green-gray light); replaces binary dark/light toggle
  • [ALL] Smooth theme transitions — all elements fade in 0.2s; interactive elements 0.15s
  • [ALL] Audio quality picker for bulk audio downloads — choose 128, 192, 320 kbps MP3 or FLAC before batch starts
  • [ALL] ETA and file size now displayed in download cards during active downloads
  • [ALL] Window size and position remembered across launches; restores correctly after monitor layout changes
  • [ALL] Advanced settings panel (collapsed by default) — Flask port, clear cache, reset all settings, reinstall ffmpeg, reinstall Deno, export/import settings
  • [ALL] What's New modal shown automatically once per version upgrade
  • [ALL] Friendly error messages for common failures — private videos, region locks, 403/404/429, premiere, members-only
  • [ALL] Orphaned .part and .ytdl files cleaned up automatically on launch
  • [ALL] Jobs cleanup thread now correctly evicts terminal-state entries after 10 minutes
  • [ALL] stderr capped at 200 lines (deque) — prevents unbounded memory growth during long downloads
  • [ALL] 5 retries and 5 fragment-retries on all platforms (was already set on Windows)
  • [ALL] Security: settings save route validates keys against a whitelist; unknown keys dropped
  • [ALL] Security: download directory checked against system path list; system paths rejected
  • [ALL] Security: HOST binding hardcoded to 127.0.0.1 — Flask never exposed to network
  • [ALL] Security: update zip URL validated against egerena.com domain before download
  • [ALL] Build numbers removed from all user-facing version displays
  • [WINDOWS] X button minimizes to system tray — downloads continue in background; tray Quit exits cleanly
  • [MAC] Splash window nodeIntegration hardened to false + contextIsolation true
  • [LINUX] Splash window nodeIntegration hardened to false + contextIsolation true


v0.94 - Build 96 (4/25/2026 9:30 AM EST)
-----------------------------------------
  • [ALL] Light theme added — Cool Slate palette, toggleable via moon/sun button in header top-right. Preference saved via localStorage and restored on next launch. Dark mode remains the default.
  • [ALL] All hardcoded surface/button/overlay colors moved to CSS variables — both themes respect every UI element including thumbnails, modals, log panel, and status badges.
  • [MAC] Settings persistence fixed — download folder, cookies, and Deno now stored in ~/Library/Application Support/EGM Downloader/ and survive app updates.
  • [MAC] App update flow improved — after downloading the update, the DMG is auto-mounted and a Finder window opens so the user only needs to drag EGM Downloader to Applications.

v0.93 - Build 94 (4/24/2026 8:55 AM EST)
----------------------------------------
  • [ALL] Version sync — Windows, macOS, and Linux aligned to v0.93 Build 94
  • [LINUX] Fixed stale GitHub repo link in app footer and INSTRUCTIONS.txt
  • [LINUX] Fixed distribution zip — now correctly includes INSTRUCTIONS.txt alongside AppImage
  • [LINUX] Removed system tray icon — the main window is now the sole entry point
  • [LINUX] Fixed window icon — previously fell back to default Electron icon; now loads the EGM logo correctly
  • [WINDOWS] Fixed installer: backend launches correctly on first run (was crashing with "code 2" due to incorrect main.js path resolution)
  • [MAC] Fixed yt-dlp and Deno install — earlier Build 94 loaded a Windows-only app.py on Mac, so ffmpeg auto-download failed, yt-dlp wasn't found, and Deno install tried to extract a Windows binary. Now uses evermeet.cx ffmpeg, Apple Silicon Deno binary, and invokes yt-dlp via bundled Python.
  • [MAC] Removed menu-bar tray icon and desktop shortcut — the main window is now the sole entry point
  • [MAC] Fixed window icon — previously fell back to default Electron icon; now loads the EGM logo correctly

EGM Downloader — Patch Notes
=============================
https://egerena.com/apps/egm.html

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  AVAILABLE ON
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ▸ Windows  https://egerena.com/apps/egm.html
  ▸ macOS    https://egerena.com/apps/egmac.html
  ▸ Linux    https://egerena.com/apps/egmlin.html
  ▸ Stay tuned at https://egerena.com/apps/egm.html
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


v0.91 (Build 88)
──────────────────────
UI IMPROVEMENT — Update check modal (ported from macOS version)

  Replaced the previous toast-based update UI with a proper centered
  modal dialog for all three update states:

  - Up to Date: shows current version, handles dev builds gracefully
  - Update Available: shows current vs latest version side by side,
    release notes panel, Update Now + Open Page + Later buttons
  - Error: clean message with close button

  Also added compareVersions() — proper semver comparison that strips
  the "v" prefix and compares each segment numerically. Correctly handles
  v0.91 vs v1.0, dev builds (current > latest), and 2-segment versions.
  Also checks build number within the same version.

  Windows-specific features intact:
  - Update Now downloads EGMd.zip, extracts installer, launches NSIS
  - Auto-quits app after installer launches

v0.91 (Build 87)
──────────────────────
BUG FIX — Cancel doesn't kill ffmpeg / ffmpeg survives app quit

Root cause: proc.kill() on Windows only terminates the exact PID it targets.
When yt-dlp spawns ffmpeg as a child process, killing yt-dlp leaves ffmpeg
running as an orphan — still encoding, still holding temp files open.
The same issue affected app quit: by the time the Electron before-quit
taskkill fired, Flask had already exited (via /api/shutdown), and the
orphaned yt-dlp+ffmpeg processes were no longer in Flask's process tree.

Fixes:
  - New _kill_proc() helper: on Windows uses taskkill /F /T /PID which
    kills the entire process tree (yt-dlp + ffmpeg children). On other
    platforms falls back to proc.kill().
  - New _active_procs registry: maps job_id → proc, maintained with a
    threading.Lock. Populated on proc start, cleared on proc exit or
    exception — never leaks.
  - cancel_download: now calls _kill_proc() instead of proc.kill()
  - /api/shutdown: now kills all active procs via _kill_proc() BEFORE
    calling os._exit(0), so no orphans survive the app quit

Applies to both audio and video downloads uniformly.
File cleanup (_cleanup) was already correct — it now fires reliably
because proc.wait() returns promptly once the full tree is dead.

v0.91 (Build 86)
──────────────────────
BUG FIX — Cards not minimizing after Download All Audio/Video

Root cause: stale DOM references in the bulk download poller.
When playlist items finish loading their full info in the background,
fetchPlaylistEntries calls renderCard() a second time using innerHTML —
this destroys and recreates the card's DOM elements (stEl, dlBtn, progEl).
The wiv setInterval poller had captured these elements at the start of
downloadOne, so it was holding detached (no-longer-in-document) nodes.
When status='done', stEl.closest('.vcard') on a detached node returns null,
minimizeDoneCard fell into the no-card branch, and the card stayed as-is.

Fix: wiv poller now re-fetches stEl, dlBtn, and progEl via getElementById
on every tick before calling applyJobProgress and applyJobDone.
Falls back to the originally captured reference if the element is gone
(e.g. item removed mid-download).

Also in this build:
- Auto-close on update: app quits after NSIS installer launches
- Footer: 'Check for Updates 🔄' moved after version number

v0.91 (Build 85)
──────────────────────
CRITICAL BUG FIX — Download All Audio deadlock

Root cause: classic subprocess pipe buffer deadlock.
When running yt-dlp with -x (audio extraction), ffmpeg writes
codec info and per-frame progress to stderr during MP3 encoding
— easily 100KB+ for a 3-minute track. The OS pipe buffer fills
(~64KB), ffmpeg blocks waiting for it to drain, yt-dlp blocks
waiting for ffmpeg, and our stdout reading loop waits forever.

Fix: stderr is now drained in a background daemon thread while
the stdout loop runs. The process can always write to stderr
without blocking, regardless of how much it outputs.

Also fixed:
  - Auto-update: app now quits automatically after NSIS installer
    launches (after 1.5s delay), so installer can replace files cleanly
  - Footer: "Check for Updates 🔄" moved to after the version number
    (now last in footer), emoji appended after text

v0.91 (Build 84)
──────────────────────
BUG FIXES

Download All Audio stuck on "Downloading"
  - Backend: added [ExtractAudio] detection alongside [Merger] and
    [VideoRemuxer] — audio conversion now correctly switches status
    to "converting" so UI shows "Converting…" with indeterminate bar
  - Frontend: when progress reaches 100% and status is still
    "downloading", UI now shows "Converting…" as a safe fallback
    instead of appearing frozen

Layout: bulk buttons no longer expand the fetch row
  - "Download All Videos" and "Download All Audio" moved to their
    own dedicated row below the fetch controls
  - Fetch row (Fetch Info, Stop Fetching, Clear, Videos, Fragments)
    stays compact and never changes size regardless of fetch state
  - Bulk action row slides in cleanly below after fetch completes

v0.91 (Build 83)
──────────────────────
NEW FEATURES

Download All Videos / Download All Audio
  - Two separate bulk download buttons replace the old "Download All"
  - "Download All Videos" forces best quality MP4 for every item
  - "Download All Audio" forces MP3 for every item, ignores card settings
  - Bulk bar and completion toast show which mode is active
  - Both buttons always shown/hidden together (single setDlAllVisible helper)

Auto-Update with Guided Installer
  - Update available toast now has "Update Now" button (Electron only)
  - Downloads EGMd.zip silently using password-protected extraction
  - Extracts egm-setup.exe, launches familiar NSIS install wizard
  - Temp files cleaned up automatically on next app startup
  - Falls back to "Open Page" button if not in Electron context

Release Notes in Update Toast
  - Update available toast now shows bullet-point release notes
  - Notes pulled from egm-version.json "notes" field (\n separated)
  - Rendered safely via textContent — no injection risk
  - Gracefully hidden when notes field is empty or missing

Check for Updates — Footer Link
  - "Check for Updates" added to footer alongside www.egerena.com and GitHub
  - Same shared checkForUpdates() function as header badge and icon
  - Three entry points (badge, icon, footer), one function, no duplication

egm-version.json updated
  - Now includes zip_url field for direct auto-download
  - notes field carries formatted bullet points for testers

UNDER THE HOOD
  - pyzipper added for AES-256 password-protected zip extraction
  - /api/download-update Flask route (download + extract + return path)
  - launch-installer IPC handler in Electron (detached spawn)
  - launchInstaller() exposed in preload.js
  - checkForUpdates() lifted to module scope (was private to IIFE)
  - 2 bugs fixed: duplicate require() in main.js, zip not cleaned on early return

v0.90 (Build 82)
──────────────────────
UI — Check for Updates icon
  - Added 🔄 icon button next to version badge in header
  - Both the version badge and icon trigger the update check
  - Hovering either brightens the icon to full opacity
  - During check: icon swaps to ⏳, badge dims
  - After check: icon restores to 🔄, opacity resets

v0.90 (Build 81)
──────────────────────
NEW FEATURE — Check for Updates
  - Version badge in header (v0.90) is now clickable
  - Hovering shows "Check for Updates" tooltip
  - Click fetches https://egerena.com/apps/egm-version.json and compares
    to the running version and build number
  - Up to date → green toast confirmation
  - Update available → amber clickable toast → opens download page
  - Network error → error toast (silent fail, no crash)
  - New backend route: /api/check-app-update
  - New CSS: .toast.warn style for amber update toast

v0.90 (Build 80)
──────────────────────
CODE AUDIT & CLEAN

Bug fix:
  - cancel_download route: removed premature job["status"] = "cancelled"
    Setting status before proc.wait() raced with the worker thread that
    does file cleanup — could cause clearCompleted to fire before temp
    files were fully removed. Worker now has sole ownership of that write.

Code quality:
  - Thread lock added to settings cache (_settings_cache) — prevents
    concurrent request corruption under heavy parallel downloads
  - UTF-8 encoding explicitly set on settings file read/write —
    fixes non-ASCII folder paths (accented chars, CJK, etc.)
  - 20 bare except: clauses → except Exception: in app.py and launch.py
  - Duplicated poll status logic extracted into shared helpers:
    applyJobProgress() and applyJobDone() — single source of truth
  - fetchAll split into fetchSingle() + fetchPlaylistEntries() helpers
    (85 lines → 49 lines, nesting depth 4-5 → 2)
  - Quit delay and toast duration replaced with named constants
    (QUIT_DELAY_MS, TOAST_DURATION)
  - import time moved to top of launch.py (was inline in exception handlers)
  - Stale "Fix N:" debug comments replaced with descriptive comments
  - Outdated cookies comment updated (said "No UI required" — wrong)
  - Dead electron-builder build config removed from package.json
  - Version bumped to v0.90

v0.89 (Build 78)
─────────────────────────────────
- Version display simplified: shows "v0.89" — hover to see full build info
- Update Plugins now shows installed versions immediately on expand (no network)
  Check for Updates button still available for latest version comparison
- NSIS Windows installer (setup.exe) — replaces raw zip delivery
  * Welcome screen, directory picker, progress bar, finish with launch option
  * Upgrade-aware: detects previous install, replaces app files, preserves
    egm_settings.json, node_bin/, runtime/deno.exe, electron/node_modules/
  * Uninstaller registered in Windows Add/Remove Programs
  * Offers to keep or remove large cached components on uninstall
  * Start Menu shortcut created automatically
- instructions.txt included: setup guide, requirements, troubleshooting
- Post-install space savings (first launch only):
  * Non-English Electron locale files removed (~15 MB)
  * npm download cache cleared after install (~50-200 MB)
- URL updated to https://egerena.com/apps/egm.html in all text files
- Package format: setup.exe delivered as EGMd.zip

v0.91 (Build 88)
──────────────────────
UI IMPROVEMENT — Update check modal (ported from macOS version)

  Replaced the previous toast-based update UI with a proper centered
  modal dialog for all three update states:

  - Up to Date: shows current version, handles dev builds gracefully
  - Update Available: shows current vs latest version side by side,
    release notes panel, Update Now + Open Page + Later buttons
  - Error: clean message with close button

  Also added compareVersions() — proper semver comparison that strips
  the "v" prefix and compares each segment numerically. Correctly handles
  v0.91 vs v1.0, dev builds (current > latest), and 2-segment versions.
  Also checks build number within the same version.

  Windows-specific features intact:
  - Update Now downloads EGMd.zip, extracts installer, launches NSIS
  - Auto-quits app after installer launches

v0.91 (Build 87)
──────────────────────
BUG FIX — Cancel doesn't kill ffmpeg / ffmpeg survives app quit

Root cause: proc.kill() on Windows only terminates the exact PID it targets.
When yt-dlp spawns ffmpeg as a child process, killing yt-dlp leaves ffmpeg
running as an orphan — still encoding, still holding temp files open.
The same issue affected app quit: by the time the Electron before-quit
taskkill fired, Flask had already exited (via /api/shutdown), and the
orphaned yt-dlp+ffmpeg processes were no longer in Flask's process tree.

Fixes:
  - New _kill_proc() helper: on Windows uses taskkill /F /T /PID which
    kills the entire process tree (yt-dlp + ffmpeg children). On other
    platforms falls back to proc.kill().
  - New _active_procs registry: maps job_id → proc, maintained with a
    threading.Lock. Populated on proc start, cleared on proc exit or
    exception — never leaks.
  - cancel_download: now calls _kill_proc() instead of proc.kill()
  - /api/shutdown: now kills all active procs via _kill_proc() BEFORE
    calling os._exit(0), so no orphans survive the app quit

Applies to both audio and video downloads uniformly.
File cleanup (_cleanup) was already correct — it now fires reliably
because proc.wait() returns promptly once the full tree is dead.

v0.91 (Build 86)
──────────────────────
BUG FIX — Cards not minimizing after Download All Audio/Video

Root cause: stale DOM references in the bulk download poller.
When playlist items finish loading their full info in the background,
fetchPlaylistEntries calls renderCard() a second time using innerHTML —
this destroys and recreates the card's DOM elements (stEl, dlBtn, progEl).
The wiv setInterval poller had captured these elements at the start of
downloadOne, so it was holding detached (no-longer-in-document) nodes.
When status='done', stEl.closest('.vcard') on a detached node returns null,
minimizeDoneCard fell into the no-card branch, and the card stayed as-is.

Fix: wiv poller now re-fetches stEl, dlBtn, and progEl via getElementById
on every tick before calling applyJobProgress and applyJobDone.
Falls back to the originally captured reference if the element is gone
(e.g. item removed mid-download).

Also in this build:
- Auto-close on update: app quits after NSIS installer launches
- Footer: 'Check for Updates 🔄' moved after version number

v0.91 (Build 85)
──────────────────────
CRITICAL BUG FIX — Download All Audio deadlock

Root cause: classic subprocess pipe buffer deadlock.
When running yt-dlp with -x (audio extraction), ffmpeg writes
codec info and per-frame progress to stderr during MP3 encoding
— easily 100KB+ for a 3-minute track. The OS pipe buffer fills
(~64KB), ffmpeg blocks waiting for it to drain, yt-dlp blocks
waiting for ffmpeg, and our stdout reading loop waits forever.

Fix: stderr is now drained in a background daemon thread while
the stdout loop runs. The process can always write to stderr
without blocking, regardless of how much it outputs.

Also fixed:
  - Auto-update: app now quits automatically after NSIS installer
    launches (after 1.5s delay), so installer can replace files cleanly
  - Footer: "Check for Updates 🔄" moved to after the version number
    (now last in footer), emoji appended after text

v0.91 (Build 84)
──────────────────────
BUG FIXES

Download All Audio stuck on "Downloading"
  - Backend: added [ExtractAudio] detection alongside [Merger] and
    [VideoRemuxer] — audio conversion now correctly switches status
    to "converting" so UI shows "Converting…" with indeterminate bar
  - Frontend: when progress reaches 100% and status is still
    "downloading", UI now shows "Converting…" as a safe fallback
    instead of appearing frozen

Layout: bulk buttons no longer expand the fetch row
  - "Download All Videos" and "Download All Audio" moved to their
    own dedicated row below the fetch controls
  - Fetch row (Fetch Info, Stop Fetching, Clear, Videos, Fragments)
    stays compact and never changes size regardless of fetch state
  - Bulk action row slides in cleanly below after fetch completes

v0.91 (Build 83)
──────────────────────
NEW FEATURES

Download All Videos / Download All Audio
  - Two separate bulk download buttons replace the old "Download All"
  - "Download All Videos" forces best quality MP4 for every item
  - "Download All Audio" forces MP3 for every item, ignores card settings
  - Bulk bar and completion toast show which mode is active
  - Both buttons always shown/hidden together (single setDlAllVisible helper)

Auto-Update with Guided Installer
  - Update available toast now has "Update Now" button (Electron only)
  - Downloads EGMd.zip silently using password-protected extraction
  - Extracts egm-setup.exe, launches familiar NSIS install wizard
  - Temp files cleaned up automatically on next app startup
  - Falls back to "Open Page" button if not in Electron context

Release Notes in Update Toast
  - Update available toast now shows bullet-point release notes
  - Notes pulled from egm-version.json "notes" field (\n separated)
  - Rendered safely via textContent — no injection risk
  - Gracefully hidden when notes field is empty or missing

Check for Updates — Footer Link
  - "Check for Updates" added to footer alongside www.egerena.com and GitHub
  - Same shared checkForUpdates() function as header badge and icon
  - Three entry points (badge, icon, footer), one function, no duplication

egm-version.json updated
  - Now includes zip_url field for direct auto-download
  - notes field carries formatted bullet points for testers

UNDER THE HOOD
  - pyzipper added for AES-256 password-protected zip extraction
  - /api/download-update Flask route (download + extract + return path)
  - launch-installer IPC handler in Electron (detached spawn)
  - launchInstaller() exposed in preload.js
  - checkForUpdates() lifted to module scope (was private to IIFE)
  - 2 bugs fixed: duplicate require() in main.js, zip not cleaned on early return

v0.90 (Build 82)
──────────────────────
UI — Check for Updates icon
  - Added 🔄 icon button next to version badge in header
  - Both the version badge and icon trigger the update check
  - Hovering either brightens the icon to full opacity
  - During check: icon swaps to ⏳, badge dims
  - After check: icon restores to 🔄, opacity resets

v0.90 (Build 81)
──────────────────────
NEW FEATURE — Check for Updates
  - Version badge in header (v0.90) is now clickable
  - Hovering shows "Check for Updates" tooltip
  - Click fetches https://egerena.com/apps/egm-version.json and compares
    to the running version and build number
  - Up to date → green toast confirmation
  - Update available → amber clickable toast → opens download page
  - Network error → error toast (silent fail, no crash)
  - New backend route: /api/check-app-update
  - New CSS: .toast.warn style for amber update toast

v0.90 (Build 80)
──────────────────────
CODE AUDIT & CLEAN

Bug fix:
  - cancel_download route: removed premature job["status"] = "cancelled"
    Setting status before proc.wait() raced with the worker thread that
    does file cleanup — could cause clearCompleted to fire before temp
    files were fully removed. Worker now has sole ownership of that write.

Code quality:
  - Thread lock added to settings cache (_settings_cache) — prevents
    concurrent request corruption under heavy parallel downloads
  - UTF-8 encoding explicitly set on settings file read/write —
    fixes non-ASCII folder paths (accented chars, CJK, etc.)
  - 20 bare except: clauses → except Exception: in app.py and launch.py
  - Duplicated poll status logic extracted into shared helpers:
    applyJobProgress() and applyJobDone() — single source of truth
  - fetchAll split into fetchSingle() + fetchPlaylistEntries() helpers
    (85 lines → 49 lines, nesting depth 4-5 → 2)
  - Quit delay and toast duration replaced with named constants
    (QUIT_DELAY_MS, TOAST_DURATION)
  - import time moved to top of launch.py (was inline in exception handlers)
  - Stale "Fix N:" debug comments replaced with descriptive comments
  - Outdated cookies comment updated (said "No UI required" — wrong)
  - Dead electron-builder build config removed from package.json
  - Version bumped to v0.90

v0.89 (Build 78)
──────────────────────
INSTALLER
  - New: setup.exe installer (NSIS MUI2) replaces manual zip extraction
    Welcome screen, directory picker, progress bar, finish with Launch checkbox
  - Upgrade-aware: detects previous install, reuses install dir, preserves
    settings (egm_settings.json) and downloaded components
  - Maintenance mode: running setup.exe on same version shows
    Yes=Repair / No=Uninstall / Cancel=Exit
  - Full uninstaller registered in Windows Add/Remove Programs
    Optionally removes downloaded components (Node.js, Electron, Deno)
    with option to keep them for faster reinstall (~350 MB)
  - Start Menu shortcut created during install
  - No admin rights required (installs to %LOCALAPPDATA%)
  - Delivery format: EGMd.zip containing setup.exe + instructions.txt

FIRST-RUN OPTIMIZATION
  - Non-English Electron locale files deleted after first npm install (~15 MB saved)
  - npm global cache cleared after first install (~50-200 MB freed)
  - Both run automatically on first launch, one time only

UI
  - Version shows as v0.89 in header and footer
    Hover over version number to see full build info (Build N, date, time)
  - Update Plugins panel shows installed versions immediately on expand
    (no network needed — Check for Updates still available for latest comparison)

v0.67.7 (Build 77)
──────────────────────
- Deno now auto-installs silently on first launch (~35 MB background download)
  No more manual install step — YouTube best quality works out of the box
  A subtle "Installing Deno…" badge appears in the Update Plugins header
  On completion: brief toast confirms success; on failure: silent fallback
- Removed the "Install Deno" toast prompt entirely

v0.67.1 (Build 76)
──────────────────────
LAUNCH & PROCESS IMPROVEMENTS
  - Fixed CMD window flash on relaunch: added CREATE_NO_WINDOW flag
    to Node.js version-check subprocess (was missing, caused brief flash)
  - Faster relaunch: Node.js version check now cached to node_bin/.node_version
    Subsequent launches skip the subprocess call entirely — noticeably snappier
  - Removed idle launcher process: pythonw.exe (launch.py) no longer waits
    for Electron to exit — it spawns Electron and exits immediately,
    removing one permanently idle process from Task Manager

v0.67.1 (Builds 74–75) ★ BIGGEST UPDATE TO DATE
─────────────────────────────────────────────────
This release is the most substantial update since the
initial build, touching every layer of the app.

STABILITY & PROCESS MANAGEMENT
  - Fixed intermittent background Python process on close
    Electron now calls /api/shutdown (clean Flask exit) then
    taskkill /F /T as a fallback — kills the full process tree
    including yt-dlp and ffmpeg children
  - Fixed app freezing when Stop Fetching is clicked mid-fetch
    UI now resets immediately on click; in-flight network
    requests continue silently in the background and clean up
    when they resolve — no more waiting, no more frozen state
  - Fixed Stop Fetching leaving orphaned stub cards for playlists
    In-flight /api/info calls check abort state on resolve and
    remove their stub cards cleanly

FETCH & PLAYLIST IMPROVEMENTS
  - Stop Fetching now works correctly for both single videos
    and large playlists at any point during the fetch
  - pendingInfo counter tracks all in-flight /api/info calls —
    kept videos are preserved, unloaded stubs are removed
  - Fixed button row overlap on narrower windows
  - Paste and Paste & Fetch moved to the card header title bar

SIZE & PERFORMANCE
  - electron-builder removed from runtime install (~150 MB saved)
    Users no longer download build tools they never need
  - icon.ico stripped of redundant BMP 256x256 entry (264 KB saved)
    Windows 10/11 uses PNG exclusively; BMP was dead weight
  - PWA manifest.json and icon-192.png removed (unused in Electron)
  - Electron window and content area resized for a tighter layout
  - npm install now uses --omit=dev, skipping all dev dependencies

UI & UX POLISH
  - Scroll-to-top arrow fixed (DOM timing bug — was never visible)
  - Scroll-to-top arrow fades in smoothly with opacity transition
  - Completed downloads move to the top of the results list
  - Clear Completed and Clear Cancelled now work correctly for
    playlists (fixed: cards not removed after DOM rebuild)
  - Clicking anywhere on a panel header toggles it open/closed
  - Panel open/closed states remembered across launches
    (Settings, Update Plugins, Cookies all persist)
  - "Quit when all downloads complete" checkbox added to Settings
  - Deno not-installed notice replaced with a 5-second toast
  - Cookies section independently collapsible within Settings
  - Desktop Shortcut button moved to Settings card header
  - Empty state icon removed, blank space reduced
  - Tooltips added to Videos and Fragments dropdowns
  - Concurrent videos default: 6, Fragments default: 4
  - Both values remembered across launches


v0.67.1 (Builds 67–73)
────────────────────────
- Site Cookies section independently collapsible within Settings
- Scroll-to-top arrow fixed and fades in smoothly
- Panel states (Settings, Update Plugins, Cookies) remembered
- "Quit when all downloads complete" in Settings
- Deno notice replaced with 5-second toast
- Concurrent videos default 6, fragments default 4
- App window resized for compact layout (max-width 760px)
- electron-builder removed from runtime (~150 MB saved)
- icon.ico BMP 256x256 removed (264 KB saved)
- manifest.json and icon-192.png removed (dead PWA artifacts)
- Paste / Paste & Fetch moved to card header title bar
- Fixed background Python process on close (taskkill /F /T)
- Fixed button row overlap on narrower windows
- Fixed Stop Fetching disappearing mid-playlist


v0.67 (Builds 64–66)
──────────────────────
- Completed downloads move to top of results list
- Clear Completed and Clear Cancelled fixed for playlists
- Desktop Shortcut button moved to Settings card header
- Update Plugins card spacing fixed
- Concurrent downloads and fragment settings remembered
- Cookies section merged into Settings card
- Empty state icon removed, blank space reduced


v0.67 (Builds 50–63)
──────────────────────
- Smart remux: AAC audio stream-copied instead of re-encoded
- --concurrent-fragments support (parallel connections per video)
- Download speed (MB/s) shown in progress badge
- "Converting..." status shown during ffmpeg merge phase
- Progress bar wired to real percentage
- Deno auto-install in Update Plugins (no admin rights required)
- bgutil-ytdlp-pot-provider for YouTube PO Token generation
- Cookies panel: upload/remove cookies.txt from within the app
- Cookies status badge always visible in Settings header
- Done cards minimised to single row; click opens download folder
- "Clear Completed" added to bulk download bar
- Scroll-to-top arrow added
- All panels collapsible; clicking header toggles them
- Version stamp: v0.67 - Build N - date/time EST


v0.67 (Builds 1–49)
─────────────────────
- Initial port from ReClip open-source project
- Flask + Electron architecture
- yt-dlp integration with 1000+ supported sites
- Per-video quality selector with real format IDs
- Playlist support (flat-playlist expansion)
- Filename editing modal before download
- Individual and bulk download with configurable concurrency
- Update Plugins panel: yt-dlp and ffmpeg update/check
- System tray icon with Quit functionality
- Pure dark navy theme with blue accent (EGM branding)
- Custom EGM icon throughout
- ffmpeg bundled (auto-downloaded, no admin required)
- Build timestamp in version string
