flask>=3.1.3
yt-dlp>=2026.8.19
bgutil-ytdlp-pot-provider
mutagen>=1.48.1
cryptography>=50.0.1
# curl_cffi ceiling <0.17.0 (0.16.x included) matches yt-dlp's supported
# range as of yt-dlp 2026.08.19 stable, which added curl_cffi 0.16.x
# support (yt-dlp changelog: "Request Handler: curl_cffi: Support
# curl_cffi 0.16.x", #17439). Confirmed via
# python3 -m yt_dlp --list-impersonate-targets with curl_cffi==0.16.0
# installed against that exact stable release: real targets listed, not
# "(unavailable)". Previously this range worked only against yt-dlp
# nightly, not stable -- that gap is now closed, since the yt-dlp floor
# above is pinned to the stable release that added support.
#
# Floor raised to 0.16.2 (curl_cffi's own release, ships an Android wheel
# per CPython minor and fixes a handful of upstream issues) -- confirmed the same
# way, curl_cffi==0.16.2 against yt-dlp 2026.08.19: real impersonate
# targets, not "(unavailable)". A patch release within the already-
# verified 0.16.x line, so this ensures fresh installs and "Update
# Plugins" pull at least this fix rather than an older 0.10.x-0.15.x
# version still technically inside the ceiling.
#
# Separately, NOT fixed by this: Kick.com VOD downloads still 404 due to
# an unrelated site-side URL scheme change on Kick's end (yt-dlp issue
# #17284, fix in progress at PR #17322, both still open as of yt-dlp
# 2026.08.19). That's a Kick extractor bug, not a curl_cffi/networking
# issue -- this pin doesn't touch it either way.
curl_cffi>=0.16.2,<0.17.0
brotli>=1.2.0
pycryptodomex>=3.23.0
websockets>=17.1
certifi>=2026.7.22
