# Release headline keywords — one per line, case-insensitive, '#' comments ignored.
# The feed validator requires at least one _version_notes bullet to mention one of
# these (so a release never ships without naming its headline feature in the notes).
#
# MAINTAINED PER RELEASE: before generating feeds, set this to the current release's
# headline term(s). For a pure maintenance release with no headline feature, leave
# this file empty (only comments) — the headline check then skips. The feed's bullet
# count is separately checked against patchnotes.txt's own count (not a fixed
# minimum), so a genuinely small release isn't blocked by this file either.
electron
